const person = {
  name: "Alice",
  age: 30,
  occupation: "Engineer"
};

function greet(person) {
  console.log(`Hello, my name is ${person.name}. I am a ${person.age}-year-old ${person.occupation}.`);
}

greet(person); 
